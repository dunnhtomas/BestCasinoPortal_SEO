{
  "name": "casino-ca-reverse-engineering",
  "version": "1.0.0",
  "description": "Comprehensive reverse engineering suite for casino.ca analysis",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug",
    "report": "playwright show-report",
    "install": "playwright install",
    "lint": "tsc --noEmit",
    "clean": "rimraf test-results dist"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "rimraf": "^5.0.0"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "cheerio": "^1.0.0-rc.12"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
